inherits:
  - duplicated
  - utils:base
  - utils:no-design-checks
  - utils:fix

mypy:
  options:
    python-version: "3.9"

ruff:
  options:
    target-version: py39
pycodestyle:
  disable:
    - N817 # camelcase, in conflict with Ruff about xml.etree.ElementTree
